services:
  backend:
    build: .
    container_name: axon-backend

    restart: unless-stopped

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - .:/app

    command: python manage.py runserver 0.0.0.0:8000
